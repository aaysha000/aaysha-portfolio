version: "3.8"

services:
  app:
    build: .
    container_name: secure_app
    user: "1001:1001"   # Run as non-root user
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
    restart: unless-stopped