version: "3.9"

services:
  portfolio:
    build: .
    container_name: my_portfolio

    ports:
      - "8081:8080"

    security_opt:
      - no-new-privileges:true

    restart: unless-stopped

    read_only: true

    tmpfs:
      - /var/cache/nginx
      - /var/run
